import { Contest, ContestWinner, UserProfile, Campaign } from '../types';

export const MOCK_ADMIN_USER: UserProfile | null = null;
export const MOCK_TESTER_USER: UserProfile | null = null;

export const MOCK_CONTESTS: Contest[] = [];
export const MOCK_CAMPAIGNS: Campaign[] = [];
